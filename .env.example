# Copy to .env and fill in your keys.
# council loads .env automatically from the project root.

# Required for live runs (OpenRouter)
OPENROUTER_API_KEY=

# Optional — only needed when embedder.provider is "openai"
OPENAI_API_KEY=
